services:
  postgres:
    image: postgres:16-alpine
    container_name: vereins-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-verein}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-verein_secret}
      POSTGRES_DB: ${POSTGRES_DB:-vereinsbestellung}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-verein} -d ${POSTGRES_DB:-vereinsbestellung}"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    image: ${GHCR_IMAGE_PREFIX:-ghcr.io/timux/food-order}/backend:${IMAGE_TAG:-latest}
    container_name: vereins-backend
    restart: unless-stopped
    pull_policy: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: postgresql://${POSTGRES_USER:-verein}:${POSTGRES_PASSWORD:-verein_secret}@postgres:5432/${POSTGRES_DB:-vereinsbestellung}
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production-use-long-random-string}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-8h}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:5173}
      TURNSTILE_SECRET_KEY: ${TURNSTILE_SECRET_KEY:-}
    ports:
      - "3001:3001"
    volumes:
      - uploads_data:/app/uploads

  frontend:
    image: ${GHCR_IMAGE_PREFIX:-ghcr.io/timux/food-order}/frontend:${IMAGE_TAG:-latest}
    container_name: vereins-frontend
    restart: unless-stopped
    pull_policy: always
    depends_on:
      - backend
    ports:
      - "5173:80"

volumes:
  postgres_data:
  uploads_data:
